import type { Metadata } from "next";
import { ContactPage } from "../ui/PublicSite";
export const metadata: Metadata = { title: "Contact Von Newman | Atlas", description: "Request an Atlas demo or contact Von Newman in Lagos, Nigeria." };
export default function Contact(){return <ContactPage/>}
