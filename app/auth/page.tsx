import { AuthExperience } from "../ui/Journeys";

export const metadata = {
  title: "Sign in — Von Newman Atlas",
  description: "Enter your organisation learning workspace.",
};

export default function AuthPage() {
  return <AuthExperience />;
}
